[build]
  command = "node scripts/inject-config.js"
  publish = "."

# Do not publish dev / API cache (optional — keeps deploy smaller)
[build.environment]
  NODE_VERSION = "20"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
